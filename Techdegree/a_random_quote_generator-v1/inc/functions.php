<?php
// PHP - Random Quote Generator

// Create the Multidimensional array of quote elements and name it quotes
// Each inner array element should be an associative array
$quotes[]= [
 <p 'quote' => 'The way to right wrongs is to turn the light of truth upon them.' </p>
 <p 'source' => 'Ida B. Wells'
</p>
];

$quotes[]= [
 <p 'quote' => 'I\'m no longer accepting the things I cannot change. I\'m changing the things I cannot accept.' </p>
 <p 'source' => 'Angela Davis'
</p>
];

$quotes[]= [
<p 'quote' => 'The best thing you can do for a person is to inspire them.' </p>
<p 'source' => 'Nipsey Hussle'
</p>
];

$quotes[]= [
 <p 'quote' => 'He who is not courageous enough to take risks will accomplish nothing in life.' </p>
 <p 'source' => 'Muhammad Ali'
</p>
];

$quotes[]= [
<p 'quote' => 'Truth cannot be concealed. Truth is \‘that which is.\’ It is specific energy in the universe. It literally begs to be revealed.' </p>
<p  'source' => 'Dr. Frances Cress Welsing'
<span 'citation' => 'The Keys to the Colors'</span>
<span 'year' => '1991' </span></p>

];


// Create the getRandomQuuote function and name it getRandomQuote
function getRandomQuote($quotes) {

};
    // generate a random number between 0 and the last index in the array parameter
    // use the random number and box notation to grab a random item from the array
    // return the random item
return getRandomQuote(){
};


// Create the printQuote funtion and name it printQuote

function printQuote() {
   // create a variable that calls the getRandomQuote() function, passing in the quotes array as an argument
   // create a variable that initiates your HTML string
   // using the template in the project instructions, add the two default quote properties
   // if the quote contains a citation value, add it the string
   // if the quote contains a year value, add it the string
   // close the string with the necessary closing HTML tags
   // display the complete HTML string
}

?>
